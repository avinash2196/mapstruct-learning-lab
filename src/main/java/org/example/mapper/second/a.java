/*
package org.example.mapper.second;

import org.example.model.testing.SecondNestedLevel1;
import org.mapstruct.Mapper;
import org.mapstruct.Mapping;
import org.mapstruct.MappingTarget;
import org.mapstruct.factory.Mappers;

@Mapper
public interface SecondNestedLevel1Mapper {
    SecondNestedLevel1Mapper INSTANCE = Mappers.getMapper(SecondNestedLevel1Mapper.class);

    SecondNestedLevel1Target mapToTarget(SecondNestedLevel1 source);

    void mapToTargetList(SecondNestedLevel1 source, @MappingTarget List<SecondNestedLevel1Target> targetList);
}

*/
